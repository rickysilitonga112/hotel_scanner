import 'package:flutter/material.dart';

class DetailInventory extends StatelessWidget {
  static const String id = 'detail_inventory';

  const DetailInventory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
